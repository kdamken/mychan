<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
<body>
  <div>
    <%- include('./partials/nav'); %>
  </div>
  <div>
    <h1><%= title %></h1>
    <% if (user) { %>
      <% if (user.member) { %>
        <p>You're already a member, go <a href="/new-post">submit a post</a>.</p>
      <% } else { %>
        <form action="/become-a-member" method="POST">
          <label for="passcode">Secret Passcode: </label>
          <input name="passcode" placeholder="username" type="text" />
          <button>Submit</button>
        </form>
      <% } %>
    <% } %>
  </div>
</body>
</html>